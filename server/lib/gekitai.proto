syntax = "proto3";

package chat;

service Gekitai {
  rpc SendMessage (Message) returns (Empty);
  rpc ReceiveMessages (Empty) returns (stream Message);
}

message Message {
  bool isSent = 1;
  string text = 2;
  string sender = 3;
}

message Empty {
  string sender = 1;
}
